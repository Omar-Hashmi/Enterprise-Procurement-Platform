import React from 'react'
import FullAppRoutes from '../../routes/AppRoutes'

// Delegate to the central route definitions to avoid duplication
export default function AppRoutes() {
  return <FullAppRoutes />
}
